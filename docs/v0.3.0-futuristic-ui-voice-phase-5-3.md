# CRONOS v0.3.0 - Fase 5.3 Preparacao Fisica de Voz

## Status

A Fase 5.3 foi executada parcialmente.

Aprovado nesta execucao:

- branch correta confirmada;
- Git limpo no inicio;
- referencia local de seguranca criada: `local-phase-5.3-start-f2f3980`;
- espaco em disco suficiente;
- whisper.cpp `v1.8.5` clonado do repositorio oficial;
- tag resolvida para commit `f24588a272ae8e23280d9c220536437164e6ed28`;
- runtime compilado localmente com Visual Studio Build Tools;
- `whisper-cli.exe` executa `--help` com exit code 0;
- DLLs locais copiadas para recursos ignorados pelo Git;
- manifest atualizado com hash real do runtime e DLLs;
- script de preparacao ganhou `-DryRun` e `-RuntimeBinDir`;
- limpeza de temporarios antigos do CRONOS adicionada ao diretorio controlado.

Bloqueado nesta execucao:

- download do `ggml-base.bin` pelo Hugging Face;
- clone Git/LFS do modelo;
- preenchimento do SHA-256 real do modelo;
- transcricao real;
- microfone real;
- teste completamente offline;
- build desktop independente com provider disponivel;
- TTS empacotado final;
- visual-test com credenciais locais.

## Causa do Bloqueio

O firewall/proxy local retornou `Endian Firewall - Cache Access Denied` para:

- `https://api.github.com/*`
- `https://huggingface.co/*`

O clone Git/LFS de `https://huggingface.co/ggerganov/whisper.cpp` ficou preso ate timeout e foi encerrado. Nenhum modelo completo foi obtido.

Nao foi usado espelho nao oficial.

## Runtime

- Projeto: `ggml-org/whisper.cpp`
- Release: `v1.8.5`
- Commit: `f24588a272ae8e23280d9c220536437164e6ed28`
- Metodo: compilacao a partir de fonte oficial
- Gerador: Visual Studio 17 2022
- Compilador: MSVC 19.44.35228.0
- SDK: Windows SDK 10.0.26100.0
- Arquitetura: x64
- Flags: `GGML_NATIVE=OFF`, `WHISPER_BUILD_TESTS=OFF`, `WHISPER_BUILD_SERVER=OFF`, `WHISPER_BUILD_EXAMPLES=ON`

Runtime preparado:

- `whisper-cli.exe`
- SHA-256: `3716AC2A3203DEF41CB49FC0CB49A03A4E4B75D7C5A1889F77164553D75FE060`
- Tamanho: 488960 bytes

DLLs copiadas:

- `ggml-base.dll`
- `ggml-cpu.dll`
- `ggml.dll`
- `whisper.dll`

Os binarios permanecem fora do Git.

## Modelo

- Modelo aprovado: `ggml-base.bin`
- Origem oficial: `https://huggingface.co/ggerganov/whisper.cpp`
- SHA publicado na tabela oficial do whisper.cpp: `465707469ff3a37a2b9b8d8f89f2f99de7299dac`
- Status local: ausente

O manifest mantem `modelSha256` como `TO_BE_FILLED_BY_PREPARE_SCRIPT`, porque o SHA-256 real do arquivo local nao pode ser computado sem o download completo.

## Manifest

Arquivo:

- `frontend/src-tauri/resources/voice/whisper/manifest.json`

Atualizado com:

- runtime real;
- commit real;
- hash real do executavel;
- hashes das DLLs;
- metodo de preparacao;
- compilador e flags;
- status do modelo como bloqueado.

## Testes Executados

- `whisper-cli.exe --help`: aprovado, exit code 0;
- `scripts/prepare-local-whisper.ps1 -DryRun`: aprovado;
- `scripts/prepare-local-whisper.ps1 -UseExistingFiles`: falha segura por modelo ausente;
- `cargo test`: aprovado, 4 testes;
- `npm run build`: aprovado.

## Resultado

A Fase 5 continua aberta.

O runtime real esta preparado, mas a validacao fisica final depende do `ggml-base.bin` obtido por origem oficial ou por procedimento administrativo autorizado que contorne o bloqueio de firewall sem usar espelho nao confiavel.
