# CRONOS v0.3.0 - Correcao do pacote local de voz

## Contexto

Build validada pelo proprietario para chat e isolamento:

- `C:\CRONOS-VALIDACAO-v0.3.0-5b01742\cronos-desktop.exe`
- Commit: `5b01742`

Ao acionar o microfone, a interface retornou:

`Manifest local de voz nao encontrado: O sistema nao pode encontrar o caminho especificado. (os error 3)`

## Diagnostico

O pacote entregue contem os arquivos de voz em:

- `C:\CRONOS-VALIDACAO-v0.3.0-5b01742\resources\voice\whisper\manifest.json`
- `C:\CRONOS-VALIDACAO-v0.3.0-5b01742\resources\voice\whisper\bin\whisper-cli.exe`
- `C:\CRONOS-VALIDACAO-v0.3.0-5b01742\resources\voice\whisper\models\ggml-base.bin`
- DLLs em `resources\voice\whisper\bin`

O codigo anterior resolvia apenas:

- `CRONOS_LOCAL_WHISPER_DIR`, quando definido;
- `app.path().resource_dir()\voice\whisper`;
- fallback de desenvolvimento via `CARGO_MANIFEST_DIR`.

Esse fluxo nao registrava os caminhos tentados, retornava erro generico de manifest ausente e podia falhar quando o `resourceDir` real do executavel entregue nao correspondia ao layout copiado manualmente para validacao.

## Correcao aplicada

- Resolver primeiro pela pasta real do executavel entregue: `<exeDir>\resources\voice\whisper`.
- Manter suporte ao `app.path().resource_dir()\voice\whisper`.
- Manter override administrativo explicito via `CRONOS_LOCAL_WHISPER_DIR`.
- Adicionar busca controlada em:
  - `%LOCALAPPDATA%\CRONOS\models\voice\whisper`
  - `%LOCALAPPDATA%\CRONOS\models\voice`
  - `%PROGRAMDATA%\CRONOS\models\voice\whisper`
  - `%PROGRAMDATA%\CRONOS\models\voice`
- Substituir erro generico por codigos:
  - `VOICE_MANIFEST_NOT_FOUND`
  - `VOICE_MANIFEST_INVALID`
  - `VOICE_RUNTIME_NOT_FOUND`
  - `VOICE_MODEL_NOT_FOUND`
  - `VOICE_RUNTIME_START_FAILED`
- Registrar origem selecionada, caminho de origem e caminhos procurados no status do provider.
- Iniciar `whisper-cli.exe` com `CREATE_NO_WINDOW` no Windows.
- Registrar execucoes em `%LOCALAPPDATA%\CRONOS\logs\whisper.log`.
- Endurecer `scripts/build-isolated.ps1` para exigir manifest, runtime, DLLs e modelo na pasta isolada entregue.

## Identidade esperada do Whisper

- Provider: `cronos-local-whisper`
- Runtime: `whisper-cli.exe`
- Runtime version: `whisper.cpp v1.8.5`
- Modelo: `ggml-base.bin`
- Modelo SHA-256: `60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe`
- Audio: `mono PCM WAV 16 kHz 16-bit`
- Origem padrao: pasta `resources` da propria build entregue

## Testes executados

- `cargo test local_whisper --lib`: aprovado, 8 testes.
- `cargo check`: aprovado.
- `powershell -File scripts\test-local-whisper-hash.ps1`: aprovado.
- `powershell -File scripts\prepare-local-whisper.ps1 -UseExistingFiles ...`: aprovado.
- `npm run build`: aprovado.
- `pytest backend\tests`: aprovado, 70 testes.

Observacao: a `.venv` local precisou receber dependencias de teste/runtime declaradas ou usadas pelos testes (`pytest`, `httpx`, dependencias do backend e `reportlab`).

## Pendencias

- Gerar nova pasta isolada com commit posterior a esta correcao.
- Executar teste direto do Whisper com WAV controlado usando o runtime da pasta entregue.
- Executar teste pelo desktop entregue.
- Nao declarar voz aprovada antes do teste manual do proprietario.
- Registrar em etapa futura a configuracao de fala automatica: sempre, nunca ou somente quando a pergunta vier por voz.

## Falha da build d98f647

O proprietario testou a build `d98f647` e o desktop passou a encontrar o manifest no caminho correto, mas falhou no parse:

`VOICE_MANIFEST_INVALID: expected value at line 1 column 1`

Arquivo inspecionado:

- `C:\CRONOS-VALIDACAO-v0.3.0-d98f647\resources\voice\whisper\manifest.json`
- Tamanho: `2809` bytes
- SHA-256: `C71080273A7FCEEA745ED61E6299038DAB00162CA0E99C3E78FB4BE3F26FA21B`
- Primeiros bytes: `EF BB BF 7B 0D 0A 20 20 20 20 22 70 72 6F 76 69 64 65 72 22 3A 20 20 22 63 72 6F 6E 6F 73 2D 6C`
- Encoding aparente: UTF-8 com BOM
- Conteudo: JSON textual valido para leitores tolerantes a BOM
- Git LFS pointer: nao
- HTML/mensagem de erro: nao
- JSON truncado: nao

Causa exata:

- `scripts\prepare-local-whisper.ps1` escrevia o manifest com:
  `ConvertTo-Json -Depth 5 | Set-Content -LiteralPath $manifestPath -Encoding UTF8`
- No Windows PowerShell usado nesta maquina, `-Encoding UTF8` grava UTF-8 com BOM.
- O desktop usa `std::fs::read_to_string` e `serde_json::from_str`.
- Esse fluxo preserva o BOM como `\uFEFF`; o parser JSON espera `{` na primeira coluna e falha antes de ler o objeto.

Correcao adicional:

- O manifest agora e escrito atomicamente em UTF-8 sem BOM via `[System.IO.File]::WriteAllText(..., [System.Text.UTF8Encoding]::new($false))`.
- A escrita ocorre primeiro em `manifest.json.tmp`.
- Antes do rename final, o script valida UTF-8 sem BOM, JSON parseavel, campos obrigatorios, caminhos relativos, arquivos existentes, SHA-256 e tamanhos.
- `scripts\build-isolated.ps1` repete a validacao no manifest ja copiado para a pasta entregue.
- `local_whisper.rs` agora inclui diagnostico de tamanho, SHA-256, encoding aparente, primeiros bytes, Git LFS e HTML em `VOICE_MANIFEST_INVALID`.
- Testes Rust foram adicionados para rejeitar UTF-8 com BOM, UTF-16, arquivo vazio, somente BOM, HTML, Git LFS pointer, JSON truncado e campos ausentes usando o mesmo parser do desktop.
