# CRONOS v0.3.0 - Fase 5.4 Aquisicao Administrativa do Modelo

## Status

A Fase 5.4 foi preparada, mas ainda nao pode ser aprovada definitivamente porque o arquivo oficial `ggml-base.bin` nao esta presente nesta maquina.

## Modelo Oficial

- Nome exigido: `ggml-base.bin`
- Variante: multilingual
- Origem oficial indicada pelo whisper.cpp: `https://huggingface.co/ggerganov/whisper.cpp`
- SHA publicado pelo whisper.cpp: `465707469ff3a37a2b9b8d8f89f2f99de7299dac`
- Tamanho esperado: 142 MiB aproximados
- Variante recusada: `ggml-base.en.bin`, por ser English-only

## Bloqueio Atual

O dominio `huggingface.co` permanece bloqueado pelo firewall local. Nenhum arquivo oficial foi encontrado localmente e nenhum espelho nao documentado foi utilizado.

## Procedimento Administrativo Preparado

O script `scripts/prepare-local-whisper.ps1` agora aceita:

- `-ImportModelPath`
- `-ModelSha256`
- `-DryRun`

Fluxo permitido:

1. Obter `ggml-base.bin` em rede autorizada a partir da origem oficial.
2. Calcular SHA-256 na origem.
3. Transferir por canal administrativo aprovado.
4. Executar o script com `-ImportModelPath`.
5. O script valida nome exato e SHA-256 antes da copia.
6. O script recalcula SHA-256 no destino.
7. Somente depois atualiza o manifest.

Exemplo sem registrar caminho pessoal:

```powershell
.\scripts\prepare-local-whisper.ps1 `
  -UseExistingFiles `
  -ImportModelPath "<canal-aprovado>\ggml-base.bin" `
  -RuntimeSha256 "3716AC2A3203DEF41CB49FC0CB49A03A4E4B75D7C5A1889F77164553D75FE060" `
  -ModelSha256 "465707469ff3a37a2b9b8d8f89f2f99de7299dac"
```

## Validacoes Adicionadas

O runtime Tauri agora recusa:

- `modelName` diferente de `ggml-base.bin`;
- `modelFile` diferente de `models/ggml-base.bin`;
- nomes contendo `.en.`;
- runtime diferente de `bin/whisper-cli.exe`.

## Testes Executados

- dry-run do script com `-ImportModelPath`: aprovado;
- parser PowerShell do script: aprovado;
- `cargo test`: aprovado, incluindo manifest English-only recusado;
- `npm run build`: aprovado;
- `cargo check`: aprovado.

## Pendente

- receber `ggml-base.bin` oficial;
- atualizar manifest sem placeholders;
- validar `get_local_whisper_status` com provider disponivel;
- executar teste direto do CLI com WAV pt-BR;
- executar microfone real;
- executar offline;
- executar build isolada;
- validar TTS empacotado;
- executar visual-test com segredos locais.

## Resultado

A Fase 5 continua aberta. A parte administrativa esta pronta, mas a validacao final depende da entrega do modelo oficial.
