# CRONOS v0.2.0 - Fase 4: Retrieval Engine

Data: 2026-07-21
Branch: `feature/v0.2.0-memory-knowledge`

## Objetivo

Criar a primeira camada desacoplada de inteligencia semantica do CRONOS para atender Biblioteca, Memoria, livros, procedimentos, programacao, conversas e aprendizado em fases futuras.

Esta fase nao implementa interface grafica, voz, automacoes, controle do computador, multiplos modelos em UI ou Qdrant.

## Arquitetura

Componentes adicionados:

- `backend/cronos/core/retrieval_config.py`
- `backend/cronos/repositories/retrieval_repository.py`
- `backend/cronos/services/embedding_provider.py`
- `backend/cronos/services/embedding_service.py`
- `backend/cronos/services/lexical_search_service.py`
- `backend/cronos/services/reranking_service.py`
- `backend/cronos/services/retrieval_service.py`

A logica permanece fora de `server.py`. O servidor apenas despacha as rotas para `library_routes.py`.

## Provider de Embeddings

Interface abstrata:

- `initialize()`
- `is_available()`
- `embed_documents()`
- `embed_query()`
- `dimension()`
- `model_name()`
- `health()`

Provider concreto padrao:

- provider: `sentence-transformers`
- modelo configurado: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- dimensao esperada: `384`

O import de `sentence_transformers` e opcional. Se o pacote/modelo nao estiver disponivel, o CRONOS continua funcionando em modo lexical, sem travar inicializacao, login, chat ou biblioteca.

Provider de teste:

- `DeterministicEmbeddingProvider`
- usado apenas nos testes para validar fluxo sem download de modelo.

## Configuracao

Centralizada em `retrieval_config.py`:

- `top_k`: 8
- `max_top_k`: 50
- `minimum_score`: 0.01
- `lexical_weight`: 0.55
- `semantic_weight`: 0.35
- `recency_weight`: 0.07
- `importance_weight`: 0.03
- `proximity_bonus`: 0.05
- `semantic_batch_size`: 32

## Embeddings

Migration adicionada:

`0004_retrieval_embeddings_schema`

Tabela `document_embeddings` consolidada com:

- `document_id`
- `chunk_id`
- `provider`
- `model`
- `dimension`
- `embedding`
- `embedding_hash`
- `created_at`
- `updated_at`

O repositório usa upsert por `chunk_id`, `provider` e `model`, evitando embeddings duplicados. Se o modelo mudar, novos embeddings podem coexistir sem quebrar os anteriores.

## Indexacao

Fluxo:

documento -> paginas -> chunks -> embedding -> armazenamento -> indexacao concluida

A indexacao e incremental:

- chunks ja indexados para o provider/modelo atual sao pulados;
- `force = true` recalcula embeddings;
- troca de provider/modelo cria novo conjunto;
- documentos sem embeddings continuam pesquisaveis lexicalmente.

## Retrieval

Fluxo de consulta:

consulta -> busca lexical -> busca semantica quando disponivel -> normalizacao -> combinacao -> reranking -> resultado

Mesmo sem embeddings, o resultado lexical permanece funcional.

Cada item retorna:

- `document`
- `page`
- `chunk`
- `citation`
- `score_lexical`
- `score_semantic`
- `score_final`
- `excerpt`

## Reranking

O reranking combina:

- score lexical;
- score semantico;
- proximidade textual;
- recencia;
- prioridade futura.

Os pesos ficam centralizados em `retrieval_config.py`, sem constantes espalhadas.

## APIs

Novas APIs:

- `POST /library/retrieve`
- `GET /library/index/status`
- `GET /library/index/providers`
- `POST /library/index/rebuild`

Entrada de `/library/retrieve`:

```json
{
  "query": "texto da consulta",
  "top_k": 8,
  "filters": {
    "document_id": 1,
    "source_id": 1,
    "page_number": 1
  }
}
```

## Compatibilidade

Documentos antigos continuam pesquisaveis. O retrieval usa os chunks existentes da Fase 3 e chama a indexacao legada quando necessario. Embeddings sao uma melhoria incremental, nao uma dependencia obrigatoria.

## Seguranca

- Conteudo de documentos nunca e executado.
- Documento nao e tratado como instrucao de sistema.
- Embeddings completos nao sao registrados em auditoria ou logs.
- Erros do provider sao sanitizados no `health`.
- O fallback lexical evita falhas de disponibilidade do modelo.

## Testes

Executado:

- `.\scripts\test.ps1`: aprovado.
  - backend: 52 testes aprovados;
  - frontend build: aprovado.
- `npm run lint`: aprovado com warning ja existente em `frontend/src/App.tsx:241`.
- `cargo check`: aprovado.
- `.\scripts\build-backend.ps1`: aprovado.
  - `cronos-backend-console.exe`: SHA-256 `72F2C51F74D5B31F7A605329FBD2EFCDA7EECF3C369DD6B5817BA78A637EBED2`
  - `cronos-backend.exe`: SHA-256 `2B143A99AAA975C3F788A9F05A9970E07AB200E165FDAD10F92AB0CD7B66F073`
  - teste do backend empacotado: aprovado.

Testes novos de retrieval:

- busca lexical;
- fallback sem modelo;
- embeddings;
- troca de provider;
- documento legado;
- reindexacao incremental;
- duplicidade de embeddings;
- modelo indisponivel;
- resultado vazio;
- reranking;
- filtros;
- compatibilidade;
- APIs;
- seguranca contra vazamento de embeddings em auditoria;
- resultado semantico sem match lexical.

## Medicao Local

Amostra com provider deterministico em ambiente temporario:

- chunks indexados: 50
- tempo de indexacao: 39,21 ms
- tempo medio de consulta: 6,55 ms
- pico medido por `tracemalloc`: 396,89 KB

Esses numeros nao representam o custo de um modelo real Sentence Transformers; medem apenas o pipeline do CRONOS com provider deterministico local.

## Impacto no PyInstaller e Instalador

Nenhuma dependencia obrigatoria nova foi adicionada. O backend empacotado continua funcionando sem `sentence_transformers`, usando fallback lexical. O tamanho do executavel backend ficou em aproximadamente 14,65 MB, mantendo o mesmo perfil de empacotamento do sidecar.

## Riscos e Pendencias

- O modelo real Sentence Transformers ainda nao e empacotado nem baixado automaticamente.
- Sem o pacote/modelo local, a busca semantica fica indisponivel e o CRONOS usa apenas lexical.
- Embeddings ficam em SQLite como JSON; Qdrant permanece fora desta fase.
- A interface ainda nao consome os novos endpoints de retrieval.
- Pesos de reranking sao iniciais e devem ser calibrados com uso real.

## Proxima Recomendacao

Antes da Fase 5, validar a politica de distribuicao do modelo local: empacotar modelo, baixar sob autorizacao, ou manter instalacao manual controlada.
